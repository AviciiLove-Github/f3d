'''
添加控件：点密度，即点一个随机生成几个，范围1-10

添加一个控件，功能是遍历public中的triggerpoint文件夹（label.json除外）,并展示里面的文件

在点击控件显示触发点结构的同时，读取label.json中的内容：例：["我-左脚","脚-右脚"]
列表的内容全都在列表中标红并下划

在列表中每个文件前加一个勾选框，若勾选，则将该json中的点云导入进来（不同文件可以叠加）

给列表加个滚轮（数据可能很多）；

读取acupoint.json，并标红和下划线对应的穴位

在列表上方添加一个刷新控件用于刷新列表状态

在右边添加一个撑满上下的聊天框（半透明），用于与后端的大模型交换数据

在刷新列表控件上添加一个“处理点云”控件，用于将点云传输给后端，暂时不写功能

给穴位列表和触发点列表加一个收起功能

再给列表里每一个元素的勾选框前加一个摄像头图标，功能是使摄像头对准glb文件对应的子节点
一次点击就能生效，并且让摄像头与子节点保持一定距离

在列表中增添搜索功能

加一个全部取消高亮的控件，同时勾选的元素要被提到列表上部
'''

'''
'''